{
  "name": "point-of-sale",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:libs": "nx run-many --target=build --projects='@libs/*' --parallel",
    "watch:libs": "nx run-many --target=build --projects='@libs/*' --parallel --watch",
    "migrate:services": "pnpm --filter './services/**' run sq-cli db:migrate",
    "seed:services": "pnpm --filter './services/**' run sq-cli db:seed:all",
    "test": "cross-env NODE_ENV=test & nx run-many --projects='@pos/*' -t test --parallel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nx/eslint": "19.5.3",
    "@nx/jest": "19.5.3",
    "cross-env": "^7.0.3",
    "eslint": "^9.5.0",
    "jest": "^29.7.0",
    "nx": "19.5.3"
  }
}